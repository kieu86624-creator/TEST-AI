import { Bookmark, Send } from 'lucide-react';import type { Project } from '../types';
export function ProjectCard({project}:{project:Project}){return <div className="card p-5"><div className="flex items-start justify-between gap-3"><div><span className="badge">{project.status.replace('_',' ')}</span><h3 className="mt-3 text-xl font-black">{project.title}</h3><p className="mt-2 text-slate-600">{project.description}</p></div><p className="rounded-2xl bg-violet/10 px-3 py-2 font-black text-violet">{project.budget}</p></div><div className="mt-4 flex flex-wrap gap-2">{project.skills.split(',').map(s=><span className="rounded-full bg-mist px-3 py-1 text-xs font-bold text-slate-500" key={s}>{s}</span>)}</div><p className="mt-4 text-sm text-slate-500">Deadline: {new Date(project.deadline).toLocaleDateString('vi-VN')} • {project.owner.companyName}</p><div className="mt-5 flex gap-2"><button className="btn-primary flex items-center gap-2"><Send className="h-4 w-4"/>Ứng tuyển</button><button className="btn-outline flex items-center gap-2"><Bookmark className="h-4 w-4"/>Lưu dự án</button></div></div>}
