import { Bell, LogOut, MessageCircle, Search } from 'lucide-react';import { Link } from 'react-router-dom';import { useAuth } from '../context/AuthContext';import { Logo } from './Logo';
export function Header(){const {user,logout}=useAuth();return <header className="fixed inset-x-0 top-0 z-40 border-b border-slate-100 bg-white/90 backdrop-blur"><div className="mx-auto flex h-20 max-w-[1200px] items-center gap-4 px-4"><Link to="/dashboard"><Logo/></Link><div className="hidden flex-1 items-center rounded-2xl bg-mist px-4 md:flex"><Search className="h-5 w-5 text-slate-400"/><input className="w-full bg-transparent px-3 py-3 outline-none" placeholder="Tìm agency, dự án, chuyên môn..."/></div><Link to="/messages" className="rounded-2xl p-3 hover:bg-mist"><MessageCircle/></Link><button className="rounded-2xl p-3 hover:bg-mist"><Bell/></button><Link to={`/profile/${user?.id}`}><img src={user?.avatar} className="h-11 w-11 rounded-2xl border object-cover"/></Link><button onClick={logout} className="rounded-2xl p-3 hover:bg-mist"><LogOut className="h-5 w-5"/></button></div></header>}
