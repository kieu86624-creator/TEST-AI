import { useEffect, useState } from 'react';import { ProjectCard } from '../components/ProjectCard';import { api } from '../lib/api';import type { Project } from '../types';
export function ProjectsPage(){const [projects,setProjects]=useState<Project[]>([]);useEffect(()=>{api<Project[]>('/projects').then(setProjects)},[]);return <div className="space-y-5"><div className="card p-5"><h1 className="text-2xl font-black">Dự án cần tìm agency</h1><p className="text-slate-500">Brand có thể đăng brief, agency có thể ứng tuyển và lưu cơ hội phù hợp.</p><button className="btn-primary mt-4">Đăng dự án mới</button></div>{projects.map(p=><ProjectCard key={p.id} project={p}/>)}</div>}
