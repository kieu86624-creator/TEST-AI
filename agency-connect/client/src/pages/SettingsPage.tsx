import { useAuth } from '../context/AuthContext';
export function SettingsPage(){const {user}=useAuth();const sections=['Thông tin tài khoản','Bảo mật','Đổi mật khẩu','Thông báo','Quyền riêng tư','Xóa tài khoản'];return <div className="card p-6"><h1 className="text-2xl font-black">Cài đặt</h1><p className="mt-1 text-slate-500">Quản lý tài khoản {user?.email}</p><div className="mt-6 space-y-3">{sections.map((s,i)=><div key={s} className={`rounded-3xl border p-5 ${i===5?'border-rose-100 bg-rose-50':'border-slate-100 bg-white'}`}><h3 className="font-black">{s}</h3><p className="mt-1 text-sm text-slate-500">Tùy chọn {s.toLowerCase()} cho tài khoản AgencyConnect.</p></div>)}</div></div>}
