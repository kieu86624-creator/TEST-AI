import 'dotenv/config';import express from 'express';import cors from 'cors';import cookieParser from 'cookie-parser';import { ZodError } from 'zod';
import { authRoutes } from './routes/authRoutes.js';import { postRoutes } from './routes/postRoutes.js';import { userRoutes } from './routes/userRoutes.js';import { partnerRoutes } from './routes/partnerRoutes.js';import { connectionRoutes } from './routes/connectionRoutes.js';import { projectRoutes } from './routes/projectRoutes.js';import { messageRoutes } from './routes/messageRoutes.js';import { errorHandler } from './utils/errors.js';
export const app = express(); app.use(cors({ origin:process.env.CLIENT_ORIGIN || 'http://localhost:5173', credentials:true })); app.use(express.json()); app.use(cookieParser());
app.get('/api/health', (_req,res)=>res.json({ ok:true, name:'AgencyConnect API' })); app.use('/api/auth', authRoutes); app.use('/api/posts', postRoutes); app.use('/api/users', userRoutes); app.use('/api/partners', partnerRoutes); app.use('/api/connections', connectionRoutes); app.use('/api/projects', projectRoutes); app.use('/api/messages', messageRoutes);
app.use((err:any, req:any, res:any, next:any)=>{ if (err instanceof ZodError) return res.status(400).json({ error:{ message:'Dữ liệu không hợp lệ', issues:err.issues }}); return errorHandler(err, req, res, next); });
